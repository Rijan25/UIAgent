[project]
name = "uiagent"
version = "0.1.0"
description = "Natural-language UI request to IR and React generator."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.42.56",
    "fastapi>=0.132.0",
    "langchain-aws>=1.3.0",
    "python-dotenv>=1.0.1",
    "pydantic>=2.12.5",
    "uvicorn>=0.35.0",
]
