ui-compiler-poc/
├── backend/
│   ├── api/
│   │   ├── server.py                 # HTTP endpoints for chat + actions
│   │   └── schemas.py                # Chat request/response DTOs
│   ├── orchestrator/
│   │   ├── pipeline.py               # main state machine
│   │   ├── state.py                  # RunState + enums
│   │   ├── transitions.py            # transition table + guards
│   │   └── handlers.py               # per-stage logic
│   ├── agents/
│   │   ├── planner/                  # LLM: requirements -> UIPlan + questions
│   │   ├── ir_producer/              # LLM: ApprovedUIPlan -> IRBundle
│   │   └── patch_producer/           # LLM: (IRBundle + user change) -> IRPatch
│   ├── contracts/
│   │   ├── ui_plan.py                # UIPlan + ApprovedUIPlan
│   │   ├── ir_bundle.py              # IRBundle + ApprovedIRBundle
│   │   └── ir_patch.py               # IRPatch ops
│   ├── ir/
│   │   ├── schemas/                  # ComponentTree/Layout/Theme/DataBehavior schemas
│   │   ├── validator.py              # deterministic checks + allowlists
│   │   ├── normalizer.py             # canonical form (sorted nodes, defaults)
│   │   ├── summarizer.py             # deterministic IR -> English summary
│   │   └── patch_apply.py            # apply IRPatch deterministically
│   ├── emitter/
│   │   ├── registry.py               # shadcn component allowlist + prop mapping
│   │   ├── emit_react.py             # IR -> TSX files (deterministic)
│   │   └── writer.py                 # atomic file writes
│   ├── preview_bridge/
│   │   └── output_paths.py           # writes into frontend/src/generated
│   ├── storage/
│   │   └── run_store.py              # persist per-run artifacts (json)
│   └── main.py                       # local CLI for debugging
│
└── frontend/                          # split-screen chat + preview route
    ├── src/
    │   ├── pages/
    │   │   ├── ChatSplit.tsx          # left chat, right iframe
    │   │   └── Preview.tsx            # renders GeneratedApp
    │   ├── generated/
    │   │   └── App.tsx                # emitter output target
    │   ├── api.ts                     # calls backend endpoints
    │   └── App.tsx                    # router: / and /preview
    └── package.json




# planner/
# ├── agent.py
# ├── prompts.py
# ├── output_schema.py
# └── utils.py (optional)